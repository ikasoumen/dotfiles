[alias]
    st = status
	ps = push
	psfwl = push --force-with-lease
	pl = pull
	sw = switch
	rs = restore
	br = branch
	cm = commit
	wdf = diff --word-diff
	lg = log --graph --date=short --decorate=short --pretty=format:'%Cgreen%h %Creset%cd %Cblue%cn %Cred%d %Creset%s'
	srt = branch --sort=authordate
	fgt = reset HEAD
[core]
	editor = code --wait
